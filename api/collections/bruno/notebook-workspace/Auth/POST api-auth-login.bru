meta {
{'type': 'http', 'name': 'Exchange credentials for a JWT', 'seq': 0}
}

vars {
{'token': '{{token}}'}
}

{{
  method: POST
  url: {{base_url}}/api/auth/login
  body: json
}

body:json {
  {
    "email": "alice@example.com",
    "password": "password123"
  }
}
